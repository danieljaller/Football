<Page x:Class="UserApp.Views.PlayerInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UserApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="PlayerInfoPage"
      DataContext="{Binding Source={StaticResource MainViewModelLocator}, Path=PlayerInfoViewModle}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Lag:" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Ålder:" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Födelsedatum:" />
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Gjorda mål:" />
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Assist:" />
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Röda kort:" />
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Gula kort:" />
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Spelade matcher:" />
        <TextBlock Grid.Row="8" Grid.Column="0" Text="Status:"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="9">
            <TextBlock Text="{Binding Team, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding Goals, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding Assists, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding RedCards, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding YellowCards, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding MatchesPlayed, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding PlayerStatus, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</Page>
